config ZMK_DEVICE_INFO
    bool "Enable ZMK device info reporting"

if ZMK_DEVICE_INFO

config ZMK_DEVICE_INFO_STUDIO_RPC
    bool "Enable ZMK device info custom Studio RPC"
    depends on ZMK_STUDIO

endif
